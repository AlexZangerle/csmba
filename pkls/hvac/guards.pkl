import "https://raw.githubusercontent.com/AlexZangerle/csmba/main/pkls/variables.pkl" as Vars
import "https://raw.githubusercontent.com/CollaborativeStateMachines/Cirrina/refs/heads/develop/src/main/resources/pkl/csm/Csml.pkl"

// This module contains all guards for hvacSM

// Guard to check if the room is too cold
roomToCold = new Csml.GuardDescription {
  expression = "\(Vars.vIndoorTemp) < \(Vars.vDesiredTemp)"
}

// Guard to check if the room is too hot
roomToHot = new Csml.GuardDescription {
  expression = "\(Vars.vIndoorTemp) > \(Vars.vDesiredTemp)"
}

// Guard to check if desired temperature has been matched
roomComfortable = new Csml.GuardDescription {
  expression = "\(Vars.vIndoorTemp) == \(Vars.vDesiredTemp)"
}

// Guard to check if event carries the current zone as data
isMyZone = new Csml.GuardDescription {
  expression = "$zoneId == \(Vars.vZoneId)"
}
